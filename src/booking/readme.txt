Пояснительная записка к тестовому заданию: Backend NodeJS Developer.

Ваше задание выполнено на базе другого бэкенда, над которым я сейчас работаю.
Ваш модуль называется booking. И находится в src/booking.
Прошу на другие модули не обращать внимание, к заданию они не относятся.

В схеме prisma (prisma/schema.prisma) созданы модели 2х баз данных - Events, Bookings.
В контроллере и сервисе (booking.controller.ts, booking.service.ts) созданы необходимые функции - createEvent(), getEvents().

Искомая функция - reserve().
Проверка на повторное бронирование выполнена как программно так и в БД.

Ответ, получаемый от сервера следующего вида:
{
    success: true,
    message: 'Booking created successfully',
    data: any
}

Базовые проверки на валидность данных сделаны, хотя что-то мог упустить из виду.